=== COPY AND PASTE THESE COMMANDS ===

// STEP 1: Configure Git (first time only)
git config --global user.name "Your Name"
git config --global user.email "youremail@example.com"

// STEP 2: Go to your project folder
cd "C:\Users\sabo-pc\Desktop\advanced web\GameProject"

// STEP 3: Initialize Git
git init

// STEP 4: Add all files
git add .

// STEP 5: Commit
git commit -m "Initial commit - NotSteam game store"

// STEP 6: Create GitHub repo (do this on github.com website)
// Then copy the commands from GitHub, should look like:

git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO-NAME.git
git branch -M main
git push -u origin main

// You'll be asked for:
// Username: your-github-username
// Password: USE YOUR PERSONAL ACCESS TOKEN (not actual password!)

=== DONE! ===

// Future updates (after first push):
git add .
git commit -m "Updated something"
git push

=== GET PERSONAL ACCESS TOKEN ===
1. Go to GitHub.com
2. Click your profile picture → Settings
3. Scroll down → Developer settings
4. Personal access tokens → Tokens (classic)
5. Generate new token (classic)
6. Name: "NotSteam"
7. Check: repo (full control)
8. Generate token
9. COPY IT (you won't see it again!)
10. Use this as password when pushing
